
---

# 📡 Yokai Monitoring Dashboard

SPA-приложение для мониторинга духовных аномалий в реальном времени.
Создано как тестовое задание для позиции **Frontend Developer**.

---

# 🚀 Стек технологий

* **Next.js 14+ (App Router)**
* **React 18**
* **Feature-Sliced Design (FSD)**
* **TanStack Query** — состояние и асинхронность
* **SCSS Modules** — стили
* **Zod** — валидация входящих данных API
* **SSE (Server-Sent Events)** — обновления в реальном времени
* **Axios**
* **Docker Compose** — запуск одной командой

---

# 🧩 Функциональность

### 1. 📋 Список аномалий (духов)

На странице **/monitoring** отображаются карточки духов:

* Имя
* Уровень угрозы (цветовая метка)
* Локация
* Статус (Active / Captured)

Данные мокируются через **Next.js Route Handlers**
(`/api/ghosts`, `/api/ghosts/events`).

---

### 2. 🎮 Взаимодействие

#### Кнопка “Capture”

При нажатии:

* Отправляется POST-запрос через TanStack Mutation
* Применяется **Optimistic Update**
* С вероятностью **30%** сервер возвращает ошибку
  → интерфейс **откатывается назад**, показывается уведомление

---

### 3. 🔴 Real-time обновления (SSE)

Раз в **5 секунд** сервер “подбрасывает событие”:

* случайный дух меняет уровень угрозы
* UI обновляется без перезагрузки страницы
* показывается toast-уведомление

---

# 🏗 Архитектура (Feature-Sliced Design)

```
app/
  monitoring/
    page.tsx
    ui/
      GhostCard/
entities/
  ghost/
    model/
    api/
    ui/
features/
  CaptureGhost/
    useCaptureGhost.ts
shared/
  api/
  ui/
  lib/
  providers/
    ReactQueryProvider.tsx
```

🟦 **entities** — термины предметной области
🟧 **features** — функциональные действия (capture)
🟩 **shared** — всё переиспользуемое
🟨 **app/** — маршрутизация и страницы Next.js

---

# 🔧 Установка зависимостей

```bash
npm install

npm install @tanstack/react-query axios zod react-hot-toast
```

Dev:

```bash
npm install -D sass
```

---

# ▶️ Запуск проекта (локально)

```bash
npm run dev
```

Открой:

```
http://localhost:3000/monitoring
```

---

# 🐳 Запуск проекта через Docker Compose

В корне проекта есть `docker-compose.yml`.

Запуск:

```bash
docker-compose up --build
```

Сервис поднимется на:

```
http://localhost:3000
```

---

# 🧪 API Endpoints

### GET `/api/ghosts`

Возвращает список духов
(валидируется через Zod)

### POST `/api/ghosts`

Мутация “Поймать духа”
— 30% шанс ошибки

### GET `/api/ghosts/events`

SSE-поток для обновления угрозы

---

# 🧱 Используемые паттерны

* React Query: `useQuery`, `useMutation`, optimistic update
* SSE EventSource + реактивное обновление кэша
* FSD-модули: entity → feature → page
* Модули SCSS без глобального загрязнения
* Zod для типизации API
* Client Providers (ReactQueryProvider)

---

# 📦 Готово для продакшн-сборки

```bash
npm run build
npm start
```

---
